
### 2026-08-17 [Pipeline] feat/refactor-extension: refactor extension state management and page UX
- Changes: background.ts is single writer for chatRoom/limit (serialized enqueueWrite, chrome.alarms 2h reset + "alarms" permission); site-one.ts messaging-only (zero storage access); sidepanel reactive via storage.onChanged with cleanup, Reload removed, readable error mapping, loading/empty/limit states; overlayRoot.tsx deleted (no overlay in build/manifest); vitest suite (5 tests) added.
- Verification: tsc --noEmit=0, plasmo build=0 (manifest: alarms ✓, overlay absent ✓), vitest 5/5, Reviewer APPROVED (98%), Co-Founder APPROVED. QA agent 2x infra failure (browser-extension launch) — mitigated by deterministic manifest/build evidence.
- Outcome: Production-ready for Chrome Web Store packaging (`pnpm --prefix clipboard-extension package`). Follow-ups flagged: broad `https://*/*` host permission narrowing, backend CORS wildcard+credentials, README store-listing polish.

### 2026-08-17 [Pipeline] feat/privacy-policy: hosted privacy policy page for CWS submission
- Changes: clipboard-web/src/app/privacy/page.tsx — static server component (no new deps), 8 sections (What We Collect / How Used / Storage & Retention / AI Processing & Third Parties / Browser Permissions / Your Controls / Contact / Last Updated), content grounded in code audit of extension+backend (facts verified with file:line evidence in specs/privacy-policy.md). Reviewer fix applied: disclosed sidepanel chat input as a second text source. specs/privacy-policy.md + clipboard-web/pnpm-workspace.yaml (pnpm sharp build approval) included.
- Verification: pnpm build=0 (/privacy listed ○ SSG), pnpm lint=0, curl /privacy=200 with all 8 headings + mailto + title, 103 lines, no "use client". Reviewer REJECTED once (chat-input omission + backend console.log concern) → chat fix applied; console.log verified dormant-Stripe-only, user-text path logs nothing. Co-Founder APPROVED (honest summary, no dark patterns).
- Outcome: Ready for deploy after merge. Flags: (1) CONTACT_EMAIL=privacy@example.com placeholder in page.tsx MUST be replaced before CWS submission; (2) deploy clipboard-web (Vercel) then set Privacy Policy URL in CWS dashboard; (3) dormant Stripe /checkout persists name+address — if payments go live, add a Payments section.

### 2026-08-18 [Pipeline] feat/ux-polish: production UX polish for CWS submission
- Changes: closed all 5 QA audit findings — sidepanel pending state (Mantine Button loading spinner + disabled textarea + runtime.lastError guard), chat auto-scroll (messagesEndRef + scrollIntoView), restyle (bg-gray-100, refined bubbles, "Reset" subtle+aria-label, "Ask AI…" placeholder, Shift+Enter newline), chrome.action badge feedback in background (AI #5c5f66 processing → ✓ #2f9e44 success / ! #e03131 error+limit, 2s clear via guarded module timer — setTimeout not alarms per ADR-1 in spec: alarms have ~30s min granularity), popup flex-column gap layout (absolute removed), single-writer restored in popup (direct storage.set removed), typed sidePanel cast, dead state + console.log removed. No new deps; site-one.ts untouched; no overlay.
- Verification: vitest 10/10 (5 new TDD badge tests), plasmo build exit 0, tsc --noEmit exit 0, grep gates all pass (placeholder/Reset/flex/no-storage-set-in-popup/no-any). Reviewer APPROVED (95%). Co-Founder APPROVED (MEDIUM tier, all 5 findings + intent/slop checks). Engineer stall on test-timing resolved via spec §Step 5 Timing resolution (badge fires in enqueueWrite microtask; assertion moved after await).
- Outcome: Production-level per QA audit bar; ready for `pnpm --prefix clipboard-extension package` + CWS submission after merge. Note: badge stuck-state if SW killed inside 2s window accepted (self-heals on next request).

### 2026-08-19 [Pipeline] feat/clipboard-v2: 2 bug fixes + 3 features + Plasmo 0.90.5
- Changes: Bug A — floating-overlay converted to proper Plasmo CSUI (default export + getStyle data-text stylesheet, manual createRoot removed) → pill styled/visible; overlayEnabled gate. Bug B — sidepanel h-full flex layout + html/body{height:100%}, input bar shrink-0 flex child (100vh root cause eliminated). Features: copy-capture toast CSUI (capturing 600ms → captured 5s auto-dismiss, Explain/Summarize → SELECTED_TEXT + clipboard write, captureOnCopy gate); manifest commands toggle-sidepanel Alt+C/Opt+C → onCommand → sidePanel.open (permission added); options page (BYO key → X-Extension-Key header, free/byo usage mode, overlay/capture toggles, write-through + debounce) + shared/settings.ts defensive parse + popup gear; background BYO mode skips limit check + increment (chatRoom still written); Plasmo 0.89.4→0.90.5 (no breaking changes hit).
- Verification: vitest 50/50 (27 existing + 23 new: settings/capture/BYO/command/header), tsc --noEmit=0, plasmo build=0, built manifest grep: sidePanel ✓ toggle-sidepanel ✓ Alt+C ✓ options_ui ✓ both content scripts ✓, messages.ts diff empty, no key/user-text logging. Reviewer APPROVED 98% + 1 post-approval hardening (onChanged listeners now parseSettings-normalize garbage writes) re-verified green by PM.
- Outcome: Production-ready on feat/clipboard-v2 (commit 9083791), merge gate with user. FLAGS: (1) backend has ZERO auth — X-Extension-Key is decorative until backend enforces it (client-side limit is advisory; follow-up before any paid/scale story); (2) QA/Co-Founder agents skipped per dispatch scope (spec→engineer→reviewer) + prior documented browser-extension launch infra failures — mitigated by deterministic manifest/build/test evidence; (3) Alt+C conflicts resolve via chrome://extensions/shortcuts.

### 2026-08-19 [Pipeline] feat/result-popover: feat(result-popover): macOS Look Up-style inline result card + sidepanel scroll fix
- Changes: sidepanel scroll root-caused to unstyled `#__plasmo` mount div (fixed via global CSS + body overflow lock); new anchored result popover CSUI (Explain/Summarize from selection pill AND copy-toast) with loading/error/retry, flip-above placement, Copy + Open-in-chat, Esc/outside/scroll dismiss; AI_ACTION storage-relay channel correlated by requestId+tabId+frameId; getErrorMessage + MessageContent moved to shared; 17 new tests.
- Verification: pnpm test 67/67 (50 preserved); pnpm build exit 0; `#__plasmo{height:100%}` present in shipped sidepanel CSS; reviewer REJECTED once (Rules-of-Hooks violation in handleRetry + duplicate CSS) → fixed and re-verified; no dependency/manifest/permission changes.
- Outcome: committed 83105d9 on feat/result-popover (worktree .worktrees/result-popover). Not merged/pushed — awaiting Entrepreneur merge decision. Pending human smoke test: load build/chrome-mv3-prod unpacked, run spec §Manual QA checklist (scroll, pill/toast flows, dismiss, flip, copy, retry, dark mode).

### 2026-08-20 [Quick Mode] style(pill): white background to match toast & popover card
- Problem: selection pill was dark (slate-900) while copy-toast and result card are white — visual mismatch.
- Changes: `floating-overlay.tsx` pill container → `bg-white/95 text-slate-900 border-slate-200`, buttons → `text-slate-600 hover:bg-slate-100`.
- Verification: 89/89 tests green; `pnpm build --zip` clean; `bg-white/95` confirmed in bundled floating-overlay js.
- Outcome: pill now matches the white design language. Committed `7afffbf` on main.

### 2026-08-20 [Quick Mode] feat(pill): show pill & copy toast for 2+ character selections
- Problem: selections ≤5 chars (e.g. "this") silently showed no pill/toast — felt like random breakage to the user.
- Changes: `CAPTURE_MIN_LENGTH` 5→1 (effective min 2 chars via `>` check) in `src/shared/capture.ts`; tests updated. Google Docs non-support documented as known limitation (canvas selection).
- Verification: 89/89 tests green; `pnpm build --zip` clean.
- Outcome: pill + copy toast now fire for any 2+ char selection. Committed `f8351d5`, pushed.

### 2026-08-20 [Quick Mode] chore: remove dead legacy icons; new icon as web favicon
- Problem: old gray/tray icons + dead constants.ts lingered; web favicon was default Next.js.
- Changes: deleted `constants.ts`, `gray.png`, `tray.png`; replaced `clipboard-web/src/app/favicon.ico` with new `icon.png` (App Router auto-detect).
- Verification: 89/89 tests green; `pnpm build --zip` clean.
- Outcome: single icon source across extension + web. Committed & pushed.

### 2026-08-21 [Pipeline] feat/clipboard-landing: complete Raycast-style landing rebuild (745da8f..a16dc0b)
- Changes: token foundation + keycap; header/hero/button restyle; hero popover mockup (command-palette spec, quota indicator per domain invariant); bento grid surface ladder; interactive demo (selection-anchored pill, mock streaming, Esc reset); privacy strip + FAQ; final CTA + footer; MotionConfig reducedMotion=user; removed dead ui/card.tsx
- Verification: pnpm lint clean, pnpm build clean (static /, 184kB), tsc clean, slop audit zero banned patterns (header backdrop-blur = spec exception), SSR curl on :4321 confirms single h1 + single stripe band; 8 conventional commits; one engineer session truncation (empty return) recovered via tighter retry
- Outcome: dev server serving at http://localhost:4321 (PID 63599); stale qa/ screenshots left uncommitted; note: /privacy route still uses legacy token names (out of scope, renders acceptably)
